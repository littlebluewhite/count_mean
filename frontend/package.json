{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test 'src/**/*.test.mjs'"
  },
  "devDependencies": {
    "happy-dom": "^20.9.0",
    "vite": "^7.0.3"
  }
}
